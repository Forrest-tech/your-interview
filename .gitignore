# --- macOS ---
.DS_Store

# --- .NET ---
bin/
obj/
*.user
*.suo
.vs/
# --- Node / Angular ---
node_modules/
dist/
.angular/
npm-debug.log*
# --- 本地基础设施数据(不入库) ---
.run/
.logs/
rabbitmq/
*.pid
*.log
# --- 密钥(本地开发用 appsettings.Development.json 内的开发密钥可入库,生产密钥走 Key Vault) ---
.env
.env.local
*.pfx
# --- 构建产物 ---
publish/
artifacts/

# 分析工作目录(临时产物,不进版本库)
.analysis/
# ⚠️ 必须锚定到仓库根,只忽略根级 storage/。
#    旧写法 `storage/` 会匹配任意深度的同名目录,误伤 C# 源码目录
#    src/Services.Assessment/Infrastructure/Storage/(内含 SpeechKeyProvider.cs、
#    IAudioStore.cs)—— 导致这两个源文件从未入库,新克隆无法编译。
/storage/
.logs/

# Erlang/RabbitMQ 崩溃转储
**/erl_crash.dump

# 本地开发配置含真实密钥(Azure Speech key / 服务账号密码), 绝不入库。
# 仓库里只提交 *.example 模板; 真值放本地副本(已被下面规则排除)。
**/appsettings.Development.json
**/appsettings.Local.json
**/appsettings.*.Local.json
**/.env
**/.env.local
.secrets-*.json
.secrets-analysis-worker-dev.json

# 但模板必须入库
!**/appsettings.Development.json.example
!**/.env.example

# 运行期用户数据:上传的录音 blob(会随使用无限增长,绝不入库)
# ⚠️ 同样必须锚定路径 —— 不能只写 storage/(见上文教训)
/src/Services.Assessment/storage/

# 本地脚本/工具(仅 Forrest 本地开发用,不入库)
# 整个 tools/ 只在 Mac 本地保留,不进 GitHub(含 dev.sh / hybrid.sh / db / rabbit 等)
/tools/

# 本地临时备份(带时间戳),绝不入库
*.bak-*
*.bak
.bak-*/

# go.sh 是本地快捷入口,依赖同为本地的 tools/stack.sh,不入库
/go.sh
*.log
